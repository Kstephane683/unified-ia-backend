fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6
requests==2.31.0
httpx==0.25.2
pillow==10.1.0
# Push navigateur (P3-PUSH, consommé par la tâche 6.5). Importé PARESSEUSEMENT,
# à l'intérieur de la fonction d'envoi : son absence est un état affiché, jamais
# une panne de démarrage — leçon de l'incident de production déjà connu.
# Version vérifiée installable en environnement vierge, sans changer aucune des
# dépendances ci-dessus (résolution pip complète ; preuve brute dans
# docs/phase3-push-subscribe/RAPPORT.md). Exige Python >= 3.10.
pywebpush==2.5.0
